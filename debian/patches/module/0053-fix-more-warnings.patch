From eb655abd1e9547d2688efde754cd12980fdeab3d Mon Sep 17 00:00:00 2001
From: Andreas Beckmann <anbe@debian.org>
Date: Sun, 8 Dec 2024 22:13:22 +0100
Subject: [PATCH] fix more warnings

---
 common/inc/nv-linux.h       | 8 ++++++++
 nvidia-drm/nvidia-drm-drv.c | 2 ++
 2 files changed, 10 insertions(+)

diff --git a/common/inc/nv-linux.h b/common/inc/nv-linux.h
index 302afb66..758fab16 100644
--- a/common/inc/nv-linux.h
+++ b/common/inc/nv-linux.h
@@ -573,21 +573,27 @@ static NvBool nv_numa_node_has_memory(int node_id)
     { \
         (ptr) = kmalloc(size, NV_GFP_KERNEL); \
         if (ptr) \
+        { \
             NV_MEMDBG_ADD(ptr, size); \
+        } \
     }
 
 #define NV_KZALLOC(ptr, size) \
     { \
         (ptr) = kzalloc(size, NV_GFP_KERNEL); \
         if (ptr) \
+        { \
             NV_MEMDBG_ADD(ptr, size); \
+        } \
     }
 
 #define NV_KMALLOC_ATOMIC(ptr, size) \
     { \
         (ptr) = kmalloc(size, NV_GFP_ATOMIC); \
         if (ptr) \
+        { \
             NV_MEMDBG_ADD(ptr, size); \
+        } \
     }
 
 #if defined(__GFP_RETRY_MAYFAIL)
@@ -602,7 +608,9 @@ static NvBool nv_numa_node_has_memory(int node_id)
     { \
         (ptr) = kmalloc(size, NV_GFP_NO_OOM); \
         if (ptr) \
+        { \
             NV_MEMDBG_ADD(ptr, size); \
+        } \
     }
 
 #define NV_KFREE(ptr, size) \
diff --git a/nvidia-drm/nvidia-drm-drv.c b/nvidia-drm/nvidia-drm-drv.c
index da2b7010..0818e36f 100644
--- a/nvidia-drm/nvidia-drm-drv.c
+++ b/nvidia-drm/nvidia-drm-drv.c
@@ -1818,12 +1818,14 @@ void nv_drm_register_drm_device(const nv_gpu_info_t *gpu_info)
     dev->dev_private = nv_dev;
     nv_dev->dev = dev;
 
+#if defined(NV_DRM_DEVICE_HAS_PDEV) || defined(NV_DRM_FBDEV_AVAILABLE)
     bool bus_is_pci =
 #if defined(NV_LINUX)
         device->bus == &pci_bus_type;
 #elif defined(NV_BSD)
         devclass_find("pci");
 #endif
+#endif
 
 #if defined(NV_DRM_DEVICE_HAS_PDEV)
     if (bus_is_pci) {
-- 
2.39.5

